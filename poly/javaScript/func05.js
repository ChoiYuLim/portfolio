// 매개변수와 아규먼트의 차이점을 구별해보자

// 함수는 이름과 동시에 값을 전달 받습니다.
// 전달 받는 변수를 매개변수
function minus(a, b) {
    return a - b;
}

// 함수를 호출
// 값을 전달, 인수(argument)
console.log(minus(1,2));

// 매개변수와 아쿠먼트의 연결고리도 확인해 봐야 한다.

